version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      # Add other environment variables from your .env file here
    volumes:
      - ./backend:/app
    restart: unless-stopped
