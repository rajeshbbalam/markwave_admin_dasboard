version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      # Add other environment variables from your .env file here
    volumes:
      - ./backend:/app
    restart: unless-stopped
